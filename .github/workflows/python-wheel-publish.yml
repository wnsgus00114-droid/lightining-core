name: Build and Publish Python Wheel

on:
  pull_request:
    branches: [ main, master ]
  push:
    branches: [ main, master ]
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      publish_target:
        description: "Publish target for manual run"
        required: true
        default: "none"
        type: choice
        options:
          - none
          - testpypi

jobs:
  build-macos-wheel:
    name: Build macOS wheel
    runs-on: macos-14
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build wheel and sdist
        run: |
          python -m pip install --upgrade pip
          python -m pip install build twine
          python -m build

      - name: Validate distributions
        run: python -m twine check dist/*

      - name: Upload dist artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-dist
          path: dist/*

  release-benchmark-gate:
    name: Release Benchmark Gate
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: macos-14
    timeout-minutes: 45
    env:
      LC_PHASE_B_CONTRACT_JSON: docs/phase_b_graph_contract.json
      LC_GRAPH_AB_WARMUP: "4"
      LC_GRAPH_AB_ITERS: "12"
      LC_GRAPH_AB_TRACE_ITERS: "6"
      LC_GRAPH_AB_FUSION_COST_MIN_SPEEDUP: "1.01"
      LC_GRAPH_AB_MIN_HOST_DISPATCH_REDUCTION_RATE_PCT: "25.0"
      LC_GRAPH_AB_MIN_CHAINED_LATENCY_REDUCTION_PCT: "15.0"
      LC_GRAPH_AB_MAX_UNSUPPORTED_RATIO_PCT: "40.0"
      LC_FUSION_MAX_CONV: "1.15"
      LC_FUSION_MAX_MATMUL: "1.90"
      LC_FUSION_MAX_ATTENTION: "2.20"
      LC_PHASE_B_EXIT_MIN_GRAPH_PIPELINE_ADOPTION_RATE_PCT: "100.0"
      LC_PHASE_B_EXIT_ALLOW_CHAIN_LATENCY_NOT_APPLICABLE: "true"
      LC_PHASE_C_CONTRACT_JSON: docs/phase_c_engine_contract.json
      LC_INTEROP_BOUNDARY_MAX_OVERHEAD_MS: "0.35"
      QB_DEVICE: cpu
      QB_WARMUP: "10"
      QB_ITERS: "40"
      QB_TRACE_ITERS: "6"
      QB_SEED: "20260401"
      QB_VAR_REPEATS: "9"
      QB_VAR_WARMUP: "20"
      QB_VAR_ITERS: "160"
      QB_VAR_TRIM_RUNS: "2"
      QB_VAR_THRESHOLD_PCT: "2.0"
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install numpy
          python -m pip install -e .

      - name: Verify Phase B contract sync
        run: |
          python scripts/generate_phase_b_contract_docs.py --check
          python scripts/check_phase_b_contract_sync.py
          python scripts/check_phase_c_contract_sync.py

      - name: Prepare release benchmark evidence directory
        run: |
          REPORT_DIR="benchmarks/reports/releases/${GITHUB_REF_NAME}"
          mkdir -p "$REPORT_DIR"
          echo "REPORT_DIR=$REPORT_DIR" >> "$GITHUB_ENV"

          cat > "$REPORT_DIR/release_bench_commands.txt" <<'CMD'
          python benchmarks/python/quick_bench.py --device ${QB_DEVICE} --warmup ${QB_WARMUP} --iters ${QB_ITERS} --seed ${QB_SEED} --out ${REPORT_DIR}/quick_bench.csv
          python benchmarks/python/quick_bench_ci_summary.py --input ${REPORT_DIR}/quick_bench.csv --json ${REPORT_DIR}/quick_bench_summary.json --md ${REPORT_DIR}/quick_bench_summary.md --regress-threshold 1.0
          python benchmarks/python/quick_bench_variance_gate.py --runs-csv ${REPORT_DIR}/quick_bench_variance_runs.csv --json ${REPORT_DIR}/quick_bench_variance_summary.json --md ${REPORT_DIR}/quick_bench_variance_summary.md --repeats ${QB_VAR_REPEATS} --variance-threshold-pct ${QB_VAR_THRESHOLD_PCT} --device ${QB_DEVICE} --warmup ${QB_VAR_WARMUP} --iters ${QB_VAR_ITERS} --seed ${QB_SEED} --trim-runs ${QB_VAR_TRIM_RUNS}
          python benchmarks/python/graph_eager_ab_bench.py --device auto --warmup ${LC_GRAPH_AB_WARMUP} --iters ${LC_GRAPH_AB_ITERS} --trace-iters ${LC_GRAPH_AB_TRACE_ITERS} --seed ${QB_SEED} --fusion-cost-min-speedup ${LC_GRAPH_AB_FUSION_COST_MIN_SPEEDUP} --require-host-dispatch-reduction --min-host-dispatch-reduction-rate-pct ${LC_GRAPH_AB_MIN_HOST_DISPATCH_REDUCTION_RATE_PCT} --require-chained-latency-reduction --min-chained-latency-reduction-pct ${LC_GRAPH_AB_MIN_CHAINED_LATENCY_REDUCTION_PCT} --require-max-unsupported-ratio-pct --max-unsupported-ratio-pct ${LC_GRAPH_AB_MAX_UNSUPPORTED_RATIO_PCT} --csv ${REPORT_DIR}/graph_eager_ab.csv --json ${REPORT_DIR}/graph_eager_ab.json --md ${REPORT_DIR}/graph_eager_ab.md
          python benchmarks/python/engine_split_bench.py --device auto --warmup ${QB_WARMUP} --iters ${QB_ITERS} --trace-iters ${QB_TRACE_ITERS} --seed ${QB_SEED} --require-shared-bench-coverage --require-max-boundary-overhead-ms --max-boundary-overhead-ms ${LC_INTEROP_BOUNDARY_MAX_OVERHEAD_MS} --out-dir ${REPORT_DIR} --pure-csv engine_split_pure_lc.csv --pure-json engine_split_pure_lc.json --interop-csv engine_split_interop.csv --interop-json engine_split_interop.json
          python benchmarks/python/fusion_pilot_bench.py --device auto --warmup 6 --iters 24 --seed 20260408 --cost-model-min-speedup ${LC_GRAPH_AB_FUSION_COST_MIN_SPEEDUP} --max-fused-over-unfused-conv ${LC_FUSION_MAX_CONV} --max-fused-over-unfused-matmul ${LC_FUSION_MAX_MATMUL} --max-fused-over-unfused-attention ${LC_FUSION_MAX_ATTENTION} --csv ${REPORT_DIR}/fusion_pilot.csv --json ${REPORT_DIR}/fusion_pilot.json --md ${REPORT_DIR}/fusion_pilot.md
          python benchmarks/python/cost_model_v2_calibration.py --device auto --warmup 4 --iters 12 --json ${REPORT_DIR}/cost_model_v2_calibration.json --md ${REPORT_DIR}/cost_model_v2_calibration.md
          python benchmarks/python/model_runner_alpha_bench.py --device auto --warmup 4 --iters 20 --csv ${REPORT_DIR}/model_runner_alpha.csv --json ${REPORT_DIR}/model_runner_alpha.json --md ${REPORT_DIR}/model_runner_alpha.md
          python benchmarks/python/phase_b_exit_audit.py --graph-json ${REPORT_DIR}/graph_eager_ab.json --contract-json docs/phase_b_graph_contract.json --out-json ${REPORT_DIR}/phase_b_exit_audit.json --out-md ${REPORT_DIR}/phase_b_exit_audit.md --bundle-json ${REPORT_DIR}/phase_b_exit_candidate_bundle.json --target-graph-pipeline-adoption-rate-pct ${LC_PHASE_B_EXIT_MIN_GRAPH_PIPELINE_ADOPTION_RATE_PCT} --allow-chain-latency-not-applicable --require-artifacts --artifact ${REPORT_DIR}/quick_bench.csv --artifact ${REPORT_DIR}/quick_bench_summary.json --artifact ${REPORT_DIR}/quick_bench_variance_summary.json --artifact ${REPORT_DIR}/graph_eager_ab.csv --artifact ${REPORT_DIR}/graph_eager_ab.json --artifact ${REPORT_DIR}/engine_split_pure_lc.csv --artifact ${REPORT_DIR}/engine_split_pure_lc.json --artifact ${REPORT_DIR}/engine_split_interop.csv --artifact ${REPORT_DIR}/engine_split_interop.json --artifact ${REPORT_DIR}/fusion_pilot.csv --artifact ${REPORT_DIR}/fusion_pilot.json --artifact ${REPORT_DIR}/release_bench_environment.json --artifact ${REPORT_DIR}/release_bench_commands.txt --readme README.md --roadmap ROADMAP.md --expected-version ${GITHUB_REF_NAME} --require-doc-sync --require-pass
          python benchmarks/python/phase_c_exit_audit.py --graph-json ${REPORT_DIR}/graph_eager_ab.json --fusion-json ${REPORT_DIR}/fusion_pilot.json --engine-interop-json ${REPORT_DIR}/engine_split_interop.json --model-runner-json ${REPORT_DIR}/model_runner_alpha.json --cost-calibration-json ${REPORT_DIR}/cost_model_v2_calibration.json --contract-json ${LC_PHASE_C_CONTRACT_JSON} --out-json ${REPORT_DIR}/phase_c_exit_audit.json --out-md ${REPORT_DIR}/phase_c_exit_audit.md --bundle-json ${REPORT_DIR}/phase_c_exit_candidate_bundle.json --require-artifacts --artifact ${REPORT_DIR}/graph_eager_ab.json --artifact ${REPORT_DIR}/fusion_pilot.json --artifact ${REPORT_DIR}/engine_split_interop.json --artifact ${REPORT_DIR}/model_runner_alpha.json --artifact ${REPORT_DIR}/cost_model_v2_calibration.json --readme README.md --roadmap ROADMAP.md --expected-version ${GITHUB_REF_NAME} --require-doc-sync --require-pass
          CMD

      - name: Capture release benchmark environment
        run: |
          python - <<'PY'
          import importlib.metadata as md
          import json
          import os
          import platform
          import sys
          from pathlib import Path

          report_dir = Path(os.environ["REPORT_DIR"])
          report_dir.mkdir(parents=True, exist_ok=True)

          def _pkg_ver(name: str) -> str:
              try:
                  return md.version(name)
              except Exception:
                  return "n/a"

          env = {
              "release_tag": os.environ.get("GITHUB_REF_NAME", ""),
              "git_ref": os.environ.get("GITHUB_REF", ""),
              "git_sha": os.environ.get("GITHUB_SHA", ""),
              "workflow": os.environ.get("GITHUB_WORKFLOW", ""),
              "run_id": os.environ.get("GITHUB_RUN_ID", ""),
              "run_attempt": os.environ.get("GITHUB_RUN_ATTEMPT", ""),
              "runner_os": os.environ.get("RUNNER_OS", ""),
              "runner_arch": os.environ.get("RUNNER_ARCH", ""),
              "python_version": sys.version,
              "platform": {
                  "system": platform.system(),
                  "release": platform.release(),
                  "version": platform.version(),
                  "machine": platform.machine(),
                  "processor": platform.processor(),
                  "platform": platform.platform(),
              },
              "packages": {
                  "lightning-core": _pkg_ver("lightning-core"),
                  "numpy": _pkg_ver("numpy"),
                  "torch": _pkg_ver("torch"),
                  "pybind11": _pkg_ver("pybind11"),
                  "scikit-build-core": _pkg_ver("scikit-build-core"),
              },
          }

          try:
              import lightning_core as lc
              env["runtime"] = {
                  "backend_name": lc.backend_name(),
                  "metal_available": bool(lc.metal_available()),
                  "cuda_available": bool(lc.cuda_available()),
              }
          except Exception as exc:
              env["runtime"] = {"error": str(exc)}

          out = report_dir / "release_bench_environment.json"
          out.write_text(json.dumps(env, indent=2), encoding="utf-8")
          print(f"saved: {out}")
          PY

      - name: Run release quick benchmark
        run: |
          python benchmarks/python/quick_bench.py \
            --device "$QB_DEVICE" \
            --warmup "$QB_WARMUP" \
            --iters "$QB_ITERS" \
            --seed "$QB_SEED" \
            --out "$REPORT_DIR/quick_bench.csv" \
            > "$REPORT_DIR/quick_bench.log"
          cat "$REPORT_DIR/quick_bench.log"

      - name: Build release quick benchmark summary
        run: |
          python benchmarks/python/quick_bench_ci_summary.py \
            --input "$REPORT_DIR/quick_bench.csv" \
            --json "$REPORT_DIR/quick_bench_summary.json" \
            --md "$REPORT_DIR/quick_bench_summary.md" \
            --regress-threshold 1.0

      - name: Run release quick benchmark variance gate
        run: |
          python benchmarks/python/quick_bench_variance_gate.py \
            --runs-csv "$REPORT_DIR/quick_bench_variance_runs.csv" \
            --json "$REPORT_DIR/quick_bench_variance_summary.json" \
            --md "$REPORT_DIR/quick_bench_variance_summary.md" \
            --repeats "$QB_VAR_REPEATS" \
            --variance-threshold-pct "$QB_VAR_THRESHOLD_PCT" \
            --device "$QB_DEVICE" \
            --warmup "$QB_VAR_WARMUP" \
            --iters "$QB_VAR_ITERS" \
            --seed "$QB_SEED" \
            --trim-runs "$QB_VAR_TRIM_RUNS"

      - name: Run release engine split benchmark
        run: |
          python benchmarks/python/engine_split_bench.py \
            --device auto \
            --warmup "$QB_WARMUP" \
            --iters "$QB_ITERS" \
            --trace-iters "$QB_TRACE_ITERS" \
            --seed "$QB_SEED" \
            --require-shared-bench-coverage \
            --require-max-boundary-overhead-ms \
            --max-boundary-overhead-ms "$LC_INTEROP_BOUNDARY_MAX_OVERHEAD_MS" \
            --out-dir "$REPORT_DIR" \
            --pure-csv engine_split_pure_lc.csv \
            --pure-json engine_split_pure_lc.json \
            --interop-csv engine_split_interop.csv \
            --interop-json engine_split_interop.json \
            > "$REPORT_DIR/engine_split_bench.log"
          cat "$REPORT_DIR/engine_split_bench.log"

      - name: Run release graph/eager A/B benchmark
        run: |
          python benchmarks/python/graph_eager_ab_bench.py \
            --device auto \
            --warmup "$LC_GRAPH_AB_WARMUP" \
            --iters "$LC_GRAPH_AB_ITERS" \
            --trace-iters "$LC_GRAPH_AB_TRACE_ITERS" \
            --seed "$QB_SEED" \
            --fusion-cost-min-speedup "$LC_GRAPH_AB_FUSION_COST_MIN_SPEEDUP" \
            --require-host-dispatch-reduction \
            --min-host-dispatch-reduction-rate-pct "$LC_GRAPH_AB_MIN_HOST_DISPATCH_REDUCTION_RATE_PCT" \
            --require-chained-latency-reduction \
            --min-chained-latency-reduction-pct "$LC_GRAPH_AB_MIN_CHAINED_LATENCY_REDUCTION_PCT" \
            --require-max-unsupported-ratio-pct \
            --max-unsupported-ratio-pct "$LC_GRAPH_AB_MAX_UNSUPPORTED_RATIO_PCT" \
            --csv "$REPORT_DIR/graph_eager_ab.csv" \
            --json "$REPORT_DIR/graph_eager_ab.json" \
            --md "$REPORT_DIR/graph_eager_ab.md" \
            > "$REPORT_DIR/graph_eager_ab.log"
          cat "$REPORT_DIR/graph_eager_ab.log"

      - name: Run release fusion pilot benchmark (v2)
        run: |
          python benchmarks/python/fusion_pilot_bench.py \
            --device auto \
            --warmup 6 \
            --iters 24 \
            --seed 20260408 \
            --cost-model-min-speedup "$LC_GRAPH_AB_FUSION_COST_MIN_SPEEDUP" \
            --max-fused-over-unfused-conv "$LC_FUSION_MAX_CONV" \
            --max-fused-over-unfused-matmul "$LC_FUSION_MAX_MATMUL" \
            --max-fused-over-unfused-attention "$LC_FUSION_MAX_ATTENTION" \
            --csv "$REPORT_DIR/fusion_pilot.csv" \
            --json "$REPORT_DIR/fusion_pilot.json" \
            --md "$REPORT_DIR/fusion_pilot.md" \
            > "$REPORT_DIR/fusion_pilot.log"
          cat "$REPORT_DIR/fusion_pilot.log"

      - name: Run release cost model v2 calibration
        run: |
          python benchmarks/python/cost_model_v2_calibration.py \
            --device auto \
            --warmup 4 \
            --iters 12 \
            --json "$REPORT_DIR/cost_model_v2_calibration.json" \
            --md "$REPORT_DIR/cost_model_v2_calibration.md" \
            > "$REPORT_DIR/cost_model_v2_calibration.log"
          cat "$REPORT_DIR/cost_model_v2_calibration.log"

      - name: Run release model runner alpha benchmark
        run: |
          python benchmarks/python/model_runner_alpha_bench.py \
            --device auto \
            --warmup 4 \
            --iters 20 \
            --csv "$REPORT_DIR/model_runner_alpha.csv" \
            --json "$REPORT_DIR/model_runner_alpha.json" \
            --md "$REPORT_DIR/model_runner_alpha.md" \
            > "$REPORT_DIR/model_runner_alpha.log"
          cat "$REPORT_DIR/model_runner_alpha.log"

      - name: Run Phase B exit audit
        run: |
          if [ "$LC_PHASE_B_EXIT_ALLOW_CHAIN_LATENCY_NOT_APPLICABLE" = "true" ]; then
            CHAIN_APPLICABLE_FLAG="--allow-chain-latency-not-applicable"
          else
            CHAIN_APPLICABLE_FLAG="--no-allow-chain-latency-not-applicable"
          fi

          python benchmarks/python/phase_b_exit_audit.py \
            --graph-json "$REPORT_DIR/graph_eager_ab.json" \
            --contract-json docs/phase_b_graph_contract.json \
            --out-json "$REPORT_DIR/phase_b_exit_audit.json" \
            --out-md "$REPORT_DIR/phase_b_exit_audit.md" \
            --bundle-json "$REPORT_DIR/phase_b_exit_candidate_bundle.json" \
            --target-graph-pipeline-adoption-rate-pct "$LC_PHASE_B_EXIT_MIN_GRAPH_PIPELINE_ADOPTION_RATE_PCT" \
            $CHAIN_APPLICABLE_FLAG \
            --require-artifacts \
            --artifact "$REPORT_DIR/quick_bench.csv" \
            --artifact "$REPORT_DIR/quick_bench_summary.json" \
            --artifact "$REPORT_DIR/quick_bench_variance_summary.json" \
            --artifact "$REPORT_DIR/graph_eager_ab.csv" \
            --artifact "$REPORT_DIR/graph_eager_ab.json" \
            --artifact "$REPORT_DIR/engine_split_pure_lc.csv" \
            --artifact "$REPORT_DIR/engine_split_pure_lc.json" \
            --artifact "$REPORT_DIR/engine_split_interop.csv" \
            --artifact "$REPORT_DIR/engine_split_interop.json" \
            --artifact "$REPORT_DIR/fusion_pilot.csv" \
            --artifact "$REPORT_DIR/fusion_pilot.json" \
            --artifact "$REPORT_DIR/release_bench_environment.json" \
            --artifact "$REPORT_DIR/release_bench_commands.txt" \
            --readme README.md \
            --roadmap ROADMAP.md \
            --expected-version "$GITHUB_REF_NAME" \
            --require-doc-sync \
            --require-pass \
            > "$REPORT_DIR/phase_b_exit_audit.log"
          cat "$REPORT_DIR/phase_b_exit_audit.log"

      - name: Run Phase C exit audit
        run: |
          python benchmarks/python/phase_c_exit_audit.py \
            --graph-json "$REPORT_DIR/graph_eager_ab.json" \
            --fusion-json "$REPORT_DIR/fusion_pilot.json" \
            --engine-interop-json "$REPORT_DIR/engine_split_interop.json" \
            --model-runner-json "$REPORT_DIR/model_runner_alpha.json" \
            --cost-calibration-json "$REPORT_DIR/cost_model_v2_calibration.json" \
            --contract-json "$LC_PHASE_C_CONTRACT_JSON" \
            --out-json "$REPORT_DIR/phase_c_exit_audit.json" \
            --out-md "$REPORT_DIR/phase_c_exit_audit.md" \
            --bundle-json "$REPORT_DIR/phase_c_exit_candidate_bundle.json" \
            --require-artifacts \
            --artifact "$REPORT_DIR/graph_eager_ab.json" \
            --artifact "$REPORT_DIR/fusion_pilot.json" \
            --artifact "$REPORT_DIR/engine_split_interop.json" \
            --artifact "$REPORT_DIR/model_runner_alpha.json" \
            --artifact "$REPORT_DIR/cost_model_v2_calibration.json" \
            --readme README.md \
            --roadmap ROADMAP.md \
            --expected-version "$GITHUB_REF_NAME" \
            --require-doc-sync \
            --require-pass \
            > "$REPORT_DIR/phase_c_exit_audit.log"
          cat "$REPORT_DIR/phase_c_exit_audit.log"

      - name: Build release benchmark manifest
        run: |
          python - <<'PY'
          import hashlib
          import json
          import os
          from pathlib import Path

          report_dir = Path(os.environ["REPORT_DIR"])
          names = [
              "quick_bench.csv",
              "quick_bench.log",
              "quick_bench_summary.json",
              "quick_bench_summary.md",
              "quick_bench_variance_runs.csv",
              "quick_bench_variance_summary.json",
              "quick_bench_variance_summary.md",
              "engine_split_pure_lc.csv",
              "engine_split_pure_lc.json",
              "engine_split_interop.csv",
              "engine_split_interop.json",
              "engine_split_bench.log",
              "graph_eager_ab.csv",
              "graph_eager_ab.json",
              "graph_eager_ab.md",
              "graph_eager_ab.log",
              "fusion_pilot.csv",
              "fusion_pilot.json",
              "fusion_pilot.md",
              "fusion_pilot.log",
              "cost_model_v2_calibration.json",
              "cost_model_v2_calibration.md",
              "cost_model_v2_calibration.log",
              "model_runner_alpha.csv",
              "model_runner_alpha.json",
              "model_runner_alpha.md",
              "model_runner_alpha.log",
              "phase_b_exit_audit.json",
              "phase_b_exit_audit.md",
              "phase_b_exit_candidate_bundle.json",
              "phase_b_exit_audit.log",
              "phase_c_exit_audit.json",
              "phase_c_exit_audit.md",
              "phase_c_exit_candidate_bundle.json",
              "phase_c_exit_audit.log",
              "release_bench_environment.json",
              "release_bench_commands.txt",
          ]
          files = []
          def _append_file(p: Path):
              h = hashlib.sha256(p.read_bytes()).hexdigest() if p.exists() else ""
              files.append(
                  {
                      "name": p.name,
                      "path": str(p),
                      "exists": p.exists(),
                      "size_bytes": p.stat().st_size if p.exists() else 0,
                      "sha256": h,
                  }
              )

          for name in names:
              p = report_dir / name
              _append_file(p)
          for p in sorted(report_dir.glob("quick_bench_run_*.csv")):
              _append_file(p)
          for p in sorted(report_dir.glob("quick_bench_run_*.log")):
              _append_file(p)
          manifest = {
              "release_tag": os.environ.get("GITHUB_REF_NAME", ""),
              "git_sha": os.environ.get("GITHUB_SHA", ""),
              "workflow_run_id": os.environ.get("GITHUB_RUN_ID", ""),
              "workflow_run_attempt": os.environ.get("GITHUB_RUN_ATTEMPT", ""),
              "artifact_format_version": 1,
              "files": files,
          }
          out = report_dir / "release_bench_manifest.json"
          out.write_text(json.dumps(manifest, indent=2), encoding="utf-8")
          print(f"saved: {out}")
          PY

      - name: Publish release benchmark summary
        run: |
          echo "## Release Benchmark Gate Summary" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- tag: \`$GITHUB_REF_NAME\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- git sha: \`$GITHUB_SHA\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- evidence dir: \`$REPORT_DIR\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- benchmark command metadata: \`$REPORT_DIR/release_bench_commands.txt\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- environment metadata: \`$REPORT_DIR/release_bench_environment.json\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- manifest: \`$REPORT_DIR/release_bench_manifest.json\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- variance gate threshold: \`CV <= ${QB_VAR_THRESHOLD_PCT}%\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- variance gate trim-runs: \`${QB_VAR_TRIM_RUNS}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- engine split evidence: \`$REPORT_DIR/engine_split_pure_lc.csv\`, \`$REPORT_DIR/engine_split_interop.csv\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- graph/eager host-dispatch gate: \`$REPORT_DIR/graph_eager_ab.csv\`, \`$REPORT_DIR/graph_eager_ab.json\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- fusion pilot evidence: \`$REPORT_DIR/fusion_pilot.csv\`, \`$REPORT_DIR/fusion_pilot.json\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- phase-b exit audit: \`$REPORT_DIR/phase_b_exit_audit.json\`, \`$REPORT_DIR/phase_b_exit_candidate_bundle.json\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- model runner alpha: \`$REPORT_DIR/model_runner_alpha.csv\`, \`$REPORT_DIR/model_runner_alpha.json\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- phase-c exit audit: \`$REPORT_DIR/phase_c_exit_audit.json\`, \`$REPORT_DIR/phase_c_exit_candidate_bundle.json\`" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          cat "$REPORT_DIR/quick_bench_summary.md" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          cat "$REPORT_DIR/quick_bench_variance_summary.md" >> "$GITHUB_STEP_SUMMARY"
          if [ -f "$REPORT_DIR/graph_eager_ab.md" ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            cat "$REPORT_DIR/graph_eager_ab.md" >> "$GITHUB_STEP_SUMMARY"
          fi
          if [ -f "$REPORT_DIR/fusion_pilot.md" ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            cat "$REPORT_DIR/fusion_pilot.md" >> "$GITHUB_STEP_SUMMARY"
          fi
          if [ -f "$REPORT_DIR/phase_b_exit_audit.md" ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            cat "$REPORT_DIR/phase_b_exit_audit.md" >> "$GITHUB_STEP_SUMMARY"
          fi
          if [ -f "$REPORT_DIR/model_runner_alpha.md" ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            cat "$REPORT_DIR/model_runner_alpha.md" >> "$GITHUB_STEP_SUMMARY"
          fi
          if [ -f "$REPORT_DIR/phase_c_exit_audit.md" ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            cat "$REPORT_DIR/phase_c_exit_audit.md" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload release benchmark evidence
        uses: actions/upload-artifact@v4
        with:
          name: release-benchmark-evidence-${{ github.ref_name }}-${{ github.run_id }}
          if-no-files-found: error
          retention-days: 30
          path: |
            ${{ env.REPORT_DIR }}/quick_bench.csv
            ${{ env.REPORT_DIR }}/quick_bench.log
            ${{ env.REPORT_DIR }}/quick_bench_summary.json
            ${{ env.REPORT_DIR }}/quick_bench_summary.md
            ${{ env.REPORT_DIR }}/quick_bench_variance_runs.csv
            ${{ env.REPORT_DIR }}/quick_bench_variance_summary.json
            ${{ env.REPORT_DIR }}/quick_bench_variance_summary.md
            ${{ env.REPORT_DIR }}/quick_bench_run_*.csv
            ${{ env.REPORT_DIR }}/quick_bench_run_*.log
            ${{ env.REPORT_DIR }}/engine_split_pure_lc.csv
            ${{ env.REPORT_DIR }}/engine_split_pure_lc.json
            ${{ env.REPORT_DIR }}/engine_split_interop.csv
            ${{ env.REPORT_DIR }}/engine_split_interop.json
            ${{ env.REPORT_DIR }}/engine_split_bench.log
            ${{ env.REPORT_DIR }}/graph_eager_ab.csv
            ${{ env.REPORT_DIR }}/graph_eager_ab.json
            ${{ env.REPORT_DIR }}/graph_eager_ab.md
            ${{ env.REPORT_DIR }}/graph_eager_ab.log
            ${{ env.REPORT_DIR }}/fusion_pilot.csv
            ${{ env.REPORT_DIR }}/fusion_pilot.json
            ${{ env.REPORT_DIR }}/fusion_pilot.md
            ${{ env.REPORT_DIR }}/fusion_pilot.log
            ${{ env.REPORT_DIR }}/cost_model_v2_calibration.json
            ${{ env.REPORT_DIR }}/cost_model_v2_calibration.md
            ${{ env.REPORT_DIR }}/cost_model_v2_calibration.log
            ${{ env.REPORT_DIR }}/model_runner_alpha.csv
            ${{ env.REPORT_DIR }}/model_runner_alpha.json
            ${{ env.REPORT_DIR }}/model_runner_alpha.md
            ${{ env.REPORT_DIR }}/model_runner_alpha.log
            ${{ env.REPORT_DIR }}/phase_b_exit_audit.json
            ${{ env.REPORT_DIR }}/phase_b_exit_audit.md
            ${{ env.REPORT_DIR }}/phase_b_exit_candidate_bundle.json
            ${{ env.REPORT_DIR }}/phase_b_exit_audit.log
            ${{ env.REPORT_DIR }}/phase_c_exit_audit.json
            ${{ env.REPORT_DIR }}/phase_c_exit_audit.md
            ${{ env.REPORT_DIR }}/phase_c_exit_candidate_bundle.json
            ${{ env.REPORT_DIR }}/phase_c_exit_audit.log
            ${{ env.REPORT_DIR }}/release_bench_environment.json
            ${{ env.REPORT_DIR }}/release_bench_commands.txt
            ${{ env.REPORT_DIR }}/release_bench_manifest.json

  publish-pypi:
    name: Publish to PyPI
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build-macos-wheel, release-benchmark-gate]
    runs-on: ubuntu-latest
    environment:
      name: pypi
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: python-dist
          path: dist

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true

  publish-testpypi:
    name: Publish to TestPyPI
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.publish_target == 'testpypi'
    needs: [build-macos-wheel]
    runs-on: ubuntu-latest
    environment:
      name: testpypi
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: python-dist
          path: dist

      - name: Publish package distributions to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true
